---
title: "Skills Platform"
description: "Installable Agent skill packs built on top of the gateway—ready to use"
icon: "sparkles"
---

import { Card, CardGroup } from '/components/Card';
import Mermaid from '/components/Mermaid';

## What are Skills?

**A Skill is an installable skill pack built on top of the gateway.** Install it into your Agent (Claude Code / OpenClaw / Codex) and the Agent immediately gains the corresponding local-life capability—for example, "ordering takeout." You don't have to stitch the call chain together from scratch; the pack already wraps a complete set of actions plus an interaction "soul."

Underneath, it still runs on the same gateway: the gateway aggregates downstream platforms (Ele.me, etc.) and local-life services, and exposes a standardized set of capabilities for menu lookup, order fulfillment, address management, and order tracking. A Skill simply packages those capabilities into a ready-to-use form for one concrete scenario.

<Mermaid
  chart={`
    graph TB
      subgraph Platforms["Downstream Platforms"]
        P1["Ele.me"]
        P2["Local-life services"]
      end

      subgraph Gateway["open-gateway"]
        G1["18 standardized tools<br/>search · menu · preview · order · status"]
      end

      subgraph Skills["Installable Skill Packs"]
        S1["takeout assistant<br/>(wrapped actions + GUIDE soul)"]
      end

      subgraph Agents["Your Agent"]
        A1["Claude Code"]
        A2["OpenClaw"]
        A3["Codex"]
      end

      P1 --> G1
      P2 --> G1
      G1 --> S1
      S1 --> A1
      S1 --> A2
      S1 --> A3

      style Platforms fill:#D1FAE5,stroke:#34D399,stroke-width:2px
      style Gateway fill:#fde8e4,stroke:#E8593C,stroke-width:2px
      style Skills fill:#f9c4bc,stroke:#D14430,stroke-width:2px
      style Agents fill:#EFF6FF,stroke:#3B82F6,stroke-width:2px
  `}
/>

## How does a Skill differ from MCP?

The two target different integration styles and complement each other:

| | **MCP (raw tools)** | **Skill (skill pack)** |
|---|---|---|
| What you get | 18 atomic tools | One wrapped capability (e.g. takeout) |
| Orchestration | You write it: chain search → menu → preview → order yourself | Pre-wrapped: actions + GUIDE wire the flow for you |
| Ramp-up | Flexible, but you define the interaction | Works out of the box—install and the capability is there |
| Best for | Fine-grained control over the lower layer, building your own Agent logic | Letting an Agent simply know how to "order takeout" |

In short: **MCP gives you the parts, a Skill gives you the finished product.** If you want to orchestrate the 18 underlying tools yourself for finer control, see the [MCP Overview](/en/mcp/overview).

## Anatomy of a Skill

Each Skill is a standardized directory with the following main parts:

| Component | Description |
|---------|------|
| **skill.yaml** | Skill metadata: name, version, auth model, environment dependencies (`requires`), action list (`actions`), supported platforms (`platforms`) |
| **GUIDE.md** | The interaction guide, the "soul" of the skill—platform-agnostic rules for when the Agent calls which action, how it talks to the user, and how it handles success/failure |
| **scripts/** | Execution scripts: the logic that actually calls the gateway (e.g. `takeout.py`, containing the open-gateway client and each action) |
| **platforms/** | Platform adaptation layer: one each for Claude Code (SKILL.md), OpenClaw (SKILL.md), and Codex (AGENTS.md) |

## Open Skills

There is currently **1 open Skill: takeout (delivery assistant)**—a full takeout flow that helps the user search shops, pick items, place orders, and track them.

<CardGroup cols={1}>
  <Card title="takeout delivery assistant" href="/en/skills/takeout" icon="utensils">
    The only open Skill: once installed, the Agent can order takeout for the user—search, item selection, preview, ordering, and tracking, end to end.
  </Card>
</CardGroup>

## One-line install

Skills are hosted on GitHub Releases; the install script downloads, verifies the sha256, and unpacks to the right location for your Agent platform. Using Claude Code as an example:

```bash
curl -fsSL https://raw.githubusercontent.com/clawdot/open-gateway-clawdot-skill/main/install.sh | bash -s -- takeout claude-code
```

After installing, drop a `.env` in the skill root with your `API_KEY` (the agent identity, `clw_` prefix, obtained when you create an Agent in the [console](https://console.hicaspian.com/agents)); `GATEWAY_URL` can stay at the default `https://eleme-gateway.hicaspian.com`. See [Installation & Platforms](/en/skills/supported-platforms) for the install and trigger details of each platform.

## Next steps

<CardGroup cols={2}>
  <Card title="takeout delivery assistant" href="/en/skills/takeout" icon="utensils">
    One-line positioning, auth, the 9 actions, the ordering model, and the interaction patterns.
  </Card>

  <Card title="Installation & Platforms" href="/en/skills/supported-platforms" icon="layout-grid">
    Install and trigger details for Claude Code / OpenClaw / Codex.
  </Card>

  <Card title="Security Model" href="/en/skills/security-model" icon="shield-check">
    Two-layer authentication, per-user self-authorization, data isolation, and credential protection.
  </Card>

  <Card title="MCP Overview" href="/en/mcp/overview" icon="wrench">
    Want to orchestrate the 18 raw tools yourself? Start here.
  </Card>
</CardGroup>
