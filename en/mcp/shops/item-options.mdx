---
title: "get_item_options"
description: "MCP tool get_item_options — batch-fetch several items' full sku candidates, each nesting its own ingredient options, with the effective order selection marked (selected)"
---

## get_item_options

Batch-fetches the **full** spec candidates of several items (`sku_options`), **each sku entry nesting its own ingredient / preparation candidates** (`sku_options[].ingredient_options`), with `selected: true` on every entry that would actually ride along on the order under the current selection. Read-only, mutates nothing, and fully decoupled from `preview_order`. **Every item in a single call must belong to the same `cart_id`** (call once per cart / shop).

Typical scenarios:

- **Adjusting an order at preview time, in bulk**: after `preview_order`, the user wants a different cup size or topping on several items in the cart — one call returns every item's skus along with each sku's own ingredient candidates; swap IDs, then simply re-run `preview_order` (`quote_cart` is optional).
- **Checking the options of several cart items at once**, without calling per-item or re-reading the whole menu response (fewer nodes to wire in an orchestration).
- **Recovering options after menu paging / filtering**: when the menu response has been pushed out of the conversation context, one call with `item_id`s brings back every option and ID for those items.
- **Enriching menu-outline items**: items from `get_shop_menu` with `full_menu=true` come without specs / ingredients and are not directly orderable — pass their `item_id`s to this tool to enrich on demand; it returns the full specs / ingredients and the same ids become orderable right away.

<Note>
  **Modification loop**: every `sku_id` / `option_id` returned here is **identical** to the IDs `get_shop_menu` minted for this cart (never re-minted), so any entry can be passed straight back into `quote_cart` / `preview_order`. After changing the selection, call `preview_order` again for a **fresh** `preview_id` + `confirmation_token` before ordering (the old tokens are single-use with a 10-minute TTL).
</Note>

### Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `consent_grant_id` | string | Yes | — | User consent grant ID (`cg_` prefix, from `verify_user_bind`), identifying the consenting user |
| `cart_id` | string | Yes | — | Cart context ID (from `search_shops`); every item in this call belongs to it |
| `items` | array | Yes | — | Items to look up, each an object with the fields below; an empty array returns `{"items": []}` |
| `items[].item_id` | string | Yes | — | Item ID (from `get_shop_menu` on the same cart) |
| `items[].sku_id` | string \| null | No | `null` | This item's currently chosen sku ID, exactly as sent to `quote_cart` / `preview_order`; omit to compute against the **default sku** |
| `items[].ingredient_option_ids` | array | No | `[]` | This item's currently chosen option ID list, exactly as sent to `quote_cart` / `preview_order`; omit to compute against the **default selection** |
| `items[].ingredient_quantities` | array | No | `[]` | This item's currently chosen portion counts, exactly as sent to `quote_cart` / `preview_order`; omit to compute 1 portion each |

### Returns

The top level is `{ "items": [...] }`, where `items` is **1:1 and in the same order** as the requested `items` (the i-th result corresponds to the i-th request entry). Each result is either a **success entry** (full spec / ingredient candidates) or a **failure entry** (`{item_id, error}`) — **distinguished by the presence of an `error` field**.

<Note>
  **A per-item failure does not affect the others.** If an item's `item_id` does not belong to the cart, or its `ingredient_option_ids` pick the same single-select required group twice, **only that entry** comes back as `{item_id, error: {code, message}}`; the remaining items are returned normally and the whole call still **succeeds**. An invalid `cart_id` / consent, however, fails the whole call (see Errors).
</Note>

<Warning>
  All money fields (`price` / `original_price` / `price_delta`) are in **cents (integer)**, not yuan. For example `price: 1600` means ¥16.00.
</Warning>

<Note>
  **`selected` vs `selected_by_default`.** `selected_by_default` says which entry is the menu default; `selected` says which entry the order would actually carry. It is computed by the same logic as the ordering path: explicit picks count, and every required group you did not touch is auto-completed with that group's default. With neither `sku_id` nor `ingredient_option_ids` supplied, `selected` shows exactly what a fully-default order carries — so you can render `selected: true` entries as "currently chosen" without missing auto-completed defaults.
</Note>

<Note>
  **Ingredients are per-sku and nested per sku.** Ingredients are bound to a specific sku (different cup sizes may offer different options), so every `sku_options[]` entry nests **that sku's own** `ingredient_options` — to switch sku, just read the target sku's nested list; no second call needed. `selected: true` only appears inside the **selected sku's** list (`sku_options[].selected: true`); other skus' candidates carry `selected_by_default` / `available` only and are always `selected: false`, since that sku would not be ordered. For **items with no sku dimension** (`sku_options` is an empty array), the single candidate set is returned as a top-level `ingredient_options` field on that result entry — present only in that case.
</Note>

<Note>
  **Options can exclude each other, and candidates narrow as you choose.** Some items layer their options — pick a coffee bean first, and only the extraction methods belonging to that bean remain selectable. Candidates that clash with a current selection come back as `available: false`, with `unavailable_reason` naming the option they clash with. Change the selection and call again; the selectable set follows.

  **Just grey out entries with `available: false`** — you do not have to work out the compatible combinations yourself. If you do send a clashing pair to `quote_cart`, it returns `can_checkout: false` and names both sides in `blocking_reason`.
</Note>

<Note>
  **Portion-style options use `ingredient_quantities`.** Options whose `max_quantity` exceeds 1 (e.g. Starbucks espresso shots) render as a stepper in the app: you can add several portions, each charged at `price_delta`. Set the count via `items[].ingredient_quantities` on `quote_cart` / `preview_order`; omit it and every option is 1 portion. Some items include a number of free portions, so trust the quoted price for the actual difference.
</Note>

**Success entry fields** (same structure as a single-item result):

| Field | Type | Description |
|-------|------|-------------|
| `items[].item_id` | string | The requested `item_id`, echoed back |
| `items[].name` | string | Item name |
| `items[].image_url` | string \| null | Item image URL, same source and value as the `get_shop_menu` / `quote_cart` / `preview_order` echoes; `null` when a cart minted before this field shipped has no cached image |
| `items[].price` | integer | Item display price (cents); for a multi-spec item this is the default / starting price — see `items[].sku_options[].price` for each spec's exact price |
| `items[].original_price` | integer \| null | Item strikethrough (original) price (cents); `null` when the item is not discounted |
| `items[].available` | boolean | Whether the item is in stock |
| `items[].min_purchase` | integer | Minimum purchase quantity, ≥ 1; `1` means no minimum |
| `items[].available_quantity` | integer \| null | Remaining stock (units): positive = units left, `0` = sold out, `null` = ample or unknown; menu-time snapshot, display only |
| `items[].promo_labels` | array | Promotion / discount labels `[{text, sub_texts}]`, e.g. `[{"text": "8.52折", "sub_texts": ["限1份"]}]`; empty array when there are none. **Purchase restrictions such as 「单点不送」 (not delivered on its own) or 「限N份」 (limit N per order) appear only in this label text** — show them before ordering. Display only, never a pricing input |
| `items[].sku_options` | array | Full spec candidates; empty array for single-spec items |
| `items[].sku_options[].sku_id` | string | Sku ID (`sku_`), usable directly as an order input |
| `items[].sku_options[].name` | string | Spec name (e.g. "Large") |
| `items[].sku_options[].price` | integer | Price of this sku (cents, after discount) |
| `items[].sku_options[].original_price` | integer \| null | Strike-through original price for this sku (cents, before discount); **`null` when this sku has no discount**. Discounts can differ across specs of the same item — read it per sku, do not reuse `items[].original_price` |
| `items[].sku_options[].promo_labels` | array | Promotion labels for THIS sku `[{text, sub_texts}]`, e.g. `[{"text": "9.2折", "sub_texts": ["限1份"]}]`; empty array when there are none. **For multi-spec items, read this one** — `items[].promo_labels` is the item card copy and may only say "from 8.59折" (the best tier), which does not tell you the discount of the spec you picked. Display only, never part of price calculation |
| `items[].sku_options[].available` | boolean | Whether this sku can be purchased |
| `items[].sku_options[].specs` | array | Spec detail `[{name, value}]`, e.g. `[{"name": "Cup size", "value": "Large"}]` |
| `items[].sku_options[].selected` | boolean | The sku the order would actually use; the default sku is marked when `sku_id` is omitted |
| `items[].sku_options[].ingredient_options` | array | Full ingredient / preparation candidates of **this sku**; entry fields below |
| `items[].sku_options[].ingredient_options[].option_id` | string | Option ID (`opt_`), usable directly in the order's `ingredient_option_ids` |
| `items[].sku_options[].ingredient_options[].group_name` | string \| null | Option group name (e.g. "Temperature", "Sugar") |
| `items[].sku_options[].ingredient_options[].name` | string | Option name |
| `items[].sku_options[].ingredient_options[].price_delta` | integer | Price delta of this option (cents) |
| `items[].sku_options[].ingredient_options[].selected_by_default` | boolean | Whether this is the menu default |
| `items[].sku_options[].ingredient_options[].available` | boolean | Whether this option can be picked **right now**; `false` when it conflicts with a current selection (see `unavailable_reason`) |
| `items[].sku_options[].ingredient_options[].unavailable_reason` | string \| null | Why it cannot be picked, naming the conflicting option; `null` when available |
| `items[].sku_options[].ingredient_options[].max_quantity` | integer | How many portions of this option can be added; `1` means pick-only, no portions |
| `items[].sku_options[].ingredient_options[].default_quantity` | integer | Merchant-recommended portion count, display only; omitting `ingredient_quantities` orders **1 portion** |
| `items[].sku_options[].ingredient_options[].selected` | boolean | Whether the order would actually carry this option; can only be `true` inside the selected sku's list |
| `items[].ingredient_options` | array | **Only for items with no sku dimension** (`sku_options` empty): the item's single candidate set, same entry structure as above |

**Failure entry fields**:

| Field | Type | Description |
|-------|------|-------------|
| `items[].item_id` | string | The request entry's `item_id`, echoed back |
| `items[].error.code` | string | Error code (e.g. `PUBLIC_REFERENCE_INVALID` / `CART_PRICE_INVALID`) |
| `items[].error.message` | string | Human-readable error description |

### Errors

| Scenario | Behavior |
|----------|----------|
| An item's `item_id` does not belong to the `cart_id` | **That entry** returns `{item_id, error}` (`PUBLIC_REFERENCE_INVALID`); the other items are unaffected |
| An item passes two options for the same single-select required group | **That entry** returns `{item_id, error}` (`CART_PRICE_INVALID`, same validation as `quote_cart` / `preview_order`); the other items are unaffected |
| `cart_id` does not exist / has expired, or consent is invalid | **The whole call fails** with HTTP 400 `PUBLIC_REFERENCE_INVALID` |

### Example

Request (the user chose a large cup at preview time and wants to see the alternatives for that item plus another item):

```json
{
  "name": "get_item_options",
  "arguments": {
    "consent_grant_id": "<consent_grant_id>",
    "cart_id": "<cart_id>",
    "items": [
      {"item_id": "<item_id_1>", "sku_id": "<currently chosen sku_id>", "ingredient_option_ids": ["<currently chosen option_id>"]},
      {"item_id": "<item_id_2>"}
    ]
  }
}
```

Response (excerpt: the first item succeeds, the second `item_id` is invalid):

```json
{
  "items": [
    {
      "item_id": "item_xxx",
      "name": "Coconut Latte",
      "image_url": "https://cube.elemecdn.com/8/be/57a834efd750f506fb634dd3f6700jpg.jpg",
      "price": 1600,
      "original_price": null,
      "available": true,
      "promo_labels": [{"text": "第2份0元", "sub_texts": ["限1份"]}],
      "sku_options": [
        {"sku_id": "sku_a", "name": "Medium", "price": 1600, "original_price": null, "available": true,
         "promo_labels": [],
         "specs": [{"name": "Cup size", "value": "Medium"}], "selected": false,
         "ingredient_options": [
           {"option_id": "opt_1", "group_name": "Temperature", "name": "Iced", "price_delta": 0,
            "selected_by_default": true, "max_quantity": 1, "default_quantity": 1, "available": true, "unavailable_reason": null, "selected": false},
           {"option_id": "opt_2", "group_name": "Temperature", "name": "Hot", "price_delta": 0,
            "selected_by_default": false, "max_quantity": 1, "default_quantity": 1, "available": true, "unavailable_reason": null, "selected": false}
         ]},
        {"sku_id": "sku_b", "name": "Large", "price": 1900, "original_price": 2100, "available": true,
         "promo_labels": [{"text": "9.05折", "sub_texts": ["限1份"]}],
         "specs": [{"name": "Cup size", "value": "Large"}], "selected": true,
         "ingredient_options": [
           {"option_id": "opt_3", "group_name": "Temperature", "name": "Iced", "price_delta": 0,
            "selected_by_default": true, "max_quantity": 1, "default_quantity": 1, "available": true, "unavailable_reason": null, "selected": true},
           {"option_id": "opt_4", "group_name": "Temperature", "name": "Hot", "price_delta": 0,
            "selected_by_default": false, "max_quantity": 1, "default_quantity": 1, "available": true, "unavailable_reason": null, "selected": false}
         ]}
      ]
    },
    {
      "item_id": "item_bad",
      "error": {"code": "PUBLIC_REFERENCE_INVALID", "message": "item_id is invalid for cart_id"}
    }
  ]
}
```
